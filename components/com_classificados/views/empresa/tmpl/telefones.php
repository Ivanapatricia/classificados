<!--
1 - Formulário com o campo:
    pais: Combobox
    Cidade : Combobox -> `id_cidade` BIGINT NOT NULL,
    `tipo` ENUM('CASA', 'CELU', 'RECA', 'COME', 'OUTR') NOT NULL,
    `ddd` VARCHAR(3) NULL,
    `telefone` VARCHAR(45) NOT NULL,
    `exibir` TINYINT(1) NULL DEFAULT 1,

    botão gravar

2 - Lista de emails adicionados
    Botão de exclusão 
    Botão de edição
    DDD
    Telefone
-->

